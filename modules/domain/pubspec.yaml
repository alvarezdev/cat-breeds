name: domain
description: A starting point for Dart libraries or applications.
version: 1.0.0

environment:
  sdk: ^3.10.8

dependencies:
  get_it: ^9.2.1
  injectable: ^3.0.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
  build_runner: ^2.4.15
  injectable_generator: